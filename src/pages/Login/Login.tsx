"use client";
import React from 'react';

export type LoginProps = {
	// types...
}

const Login: React.FC<LoginProps>  = ({}) => {
	return (
		<div>
 			Login works!
 		</div>
	);
};

export default Login;
